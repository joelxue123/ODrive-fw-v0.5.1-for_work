
修改内容：
2024.5.7 
1、增加金刚科技485通信
2、{ （8400-8000）+ 7000 } * （1/168） = 41us。一共50us。 资源利用率太高。 需降低。 
3、增加时间戳，有异响的问题，需要解决。
2024.5.8
1、修改void ADC_IRQHandler(void)，移除ADC_IRQ_Dispatch(&hadc1, &pwm_trig_adc_cb);ADC_IRQ_Dispatch(&hadc2, &pwm_trig_adc_cb); 485通信，需要在发送完成后迅速把EN引脚拉低，这个造成了丑陋的代码逻辑，想要彻底解决这个问题，需要用支持自动收发引进的MCU，或者为它配置定时器pwm。

未做列表：
1、最近odrive固件，将电流环放入中断，更合理。需要引入
2、MCU 栈资源，CPU使用率未统计。这是隐患。
